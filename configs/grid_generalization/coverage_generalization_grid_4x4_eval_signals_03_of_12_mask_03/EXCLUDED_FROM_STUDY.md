# Excluded duplicate mask

This generated diagnostic grid is preserved for provenance but excluded from evaluation because its
three signalized junctions duplicate `coverage_generalization_grid_4x4_eval_signals_03_of_12_mask_01`.
The distinct `mask_06` scenario replaces it in the authoritative evaluation configuration.
